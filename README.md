# Документация по установке и запуску проекта

##  Системные требования

### Обязательные:
- **Python 3.8 или выше**
- **PostgreSQL 13+** (для production) или **SQLite** (для разработки)
- **Git** (для клонирования репозитория)

### Рекомендуемые:
- **PyCharm** или другой IDE для Python
- **pgAdmin 4** для управления PostgreSQL

##  Полная инструкция по установке

### Шаг 1: Клонирование репозитория
```bash
# Клонируйте репозиторий с GitHub
git clone https://github.com/ваш-логин/social-network.git

# Перейдите в папку проекта
cd social-network
```

### Шаг 2: Создание виртуального окружения

**Для Windows:**
```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
venv\Scripts\activate
```

**Для Linux/Mac:**
```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate
```

** Проверка:** В начале строки терминала должно появиться `(venv)`

### Шаг 3: Установка зависимостей

```bash
# Установите все необходимые пакеты
pip install django==3.2
pip install djangorestframework
pip install psycopg2-binary
pip install Pillow
pip install python-dotenv
pip install django-cors-headers
```

### Шаг 4: Настройка базы данных

#### Вариант A: Использовать SQLite (рекомендуется для разработки)
Никакой дополнительной настройки не требуется. Django автоматически создаст файл `db.sqlite3`.

#### Вариант B: Использовать PostgreSQL (требуется для production)
1. **Создайте базу данных в PostgreSQL:**
   ```sql
   CREATE DATABASE social_network;
   ```

2. **Создайте файл `.env` в корне проекта:**
   ```env
   DB_NAME=social_network
   DB_USER=postgres
   DB_PASSWORD=ваш_пароль_от_postgres
   DB_HOST=localhost
   DB_PORT=5432
   SECRET_KEY=ваш_секретный_ключ
   DEBUG=True
   ALLOWED_HOSTS=localhost,127.0.0.1
   ```

   **Сгенерировать SECRET_KEY:**
   ```bash
   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```

### Шаг 5: Настройка проекта Django

```bash
# Примените миграции базы данных
python manage.py makemigrations posts
python manage.py migrate

# Создайте администратора
python manage.py createsuperuser
# Введите:
# - Username: admin
# - Email: admin@example.com  
# - Password: admin123 (или более сложный)
```

### Шаг 6: Запуск сервера разработки

```bash
# Запустите сервер
python manage.py runserver
```

### Шаг 7: Проверка работы

1. **Откройте браузер**
2. **Перейдите по адресу:** http://127.0.0.1:8000/
3. **Вы должны увидеть:**
   - Главную страницу проекта
   - Информацию об API
   - Ссылки на админ-панель и API

4. **Админ-панель:** http://127.0.0.1:8000/admin/
   - Войдите с логином `admin` и паролем `admin123`

## 📡 Работа с API

### Основные эндпоинты:

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/posts/` | Получить список всех постов |
| POST | `/api/posts/` | Создать новый пост (требует аутентификации) |
| GET | `/api/posts/{id}/` | Получить детальную информацию о посте |
| POST | `/api/posts/{id}/like/` | Поставить лайк посту |
| POST | `/api/posts/{id}/unlike/` | Убрать лайк |
| GET | `/api/comments/` | Получить все комментарии |
| POST | `/api/comments/` | Создать комментарий |

### Аутентификация в API:
- **Вход:** http://127.0.0.1:8000/api-auth/login/
- **Выход:** http://127.0.0.1:8000/api-auth/logout/

### Пример запроса к API:
```bash
# Получить все посты
curl http://127.0.0.1:8000/api/posts/

# Создать пост (с аутентификацией)
curl -X POST http://127.0.0.1:8000/api/posts/ \
  -H "Authorization: Basic $(echo -n 'admin:admin123' | base64)" \
  -F "text=Мой новый пост" \
  -F "image=@photo.jpg"
```

## 🗄️ Структура проекта

```
social-network/
├── .env                    # Переменные окружения (не коммитить!)
├── .gitignore             # Игнорируемые файлы Git
├── manage.py              # Основной скрипт управления Django
├── requirements.txt       # Список зависимостей Python
├── db.sqlite3             # База данных SQLite (создается автоматически)
├── social_network/        # Основная папка проекта Django
│   ├── __init__.py
│   ├── settings.py       # Настройки проекта
│   ├── urls.py          # Маршруты URL
│   └── wsgi.py          # WSGI конфигурация
├── posts/                # Приложение для постов
│   ├── migrations/       # Файлы миграций базы данных
│   ├── __init__.py
│   ├── admin.py         # Настройки админ-панели
│   ├── apps.py
│   ├── models.py        # Модели данных (Post, Comment, Like)
│   ├── serializers.py   # Сериализаторы для API
│   ├── tests.py         # Тесты
│   └── views.py         # Представления API
└── media/                # Загруженные файлы (создается автоматически)
    └── posts/images/    # Изображения постов
```

##  Создание тестовых данных

```bash
# Запустите Django shell
python manage.py shell
```

```python
from django.contrib.auth.models import User
from posts.models import Post, Comment, Like

# Создайте пользователей
user1 = User.objects.create_user('alena', 'alena@example.com', 'password123')
user2 = User.objects.create_user('semen', 'semen@example.com', 'password123')

# Создайте пост
post = Post.objects.create(
    author=user1,
    text='Делюсь с вами фото моего отпуска на Красной Поляне. Было невероятно!'
)

# Добавьте комментарий
Comment.objects.create(
    post=post,
    author=user2,
    text='Красота!!!'
)

# Добавьте лайк
Like.objects.create(post=post, user=user2)

print(f'Создано: 2 пользователя, 1 пост, 1 комментарий, 1 лайк')
```


## 📝 Дополнительные команды

### Проверка состояния проекта:
```bash
# Проверить настройки Django
python manage.py check

# Показать все миграции
python manage.py showmigrations

# Создать резервную копию данных
python manage.py dumpdata > backup.json

# Восстановить из резервной копии
python manage.py loaddata backup.json
```

### Управление статическими файлами:
```bash
# Собрать статические файлы (для production)
python manage.py collectstatic
```
